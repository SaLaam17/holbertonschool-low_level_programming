#include "main.h"

/**
 * print_sign - Entry point
 * @n: input
 * Description: int _islower checks for lowercase character.
 * Return: 1, 0, -1
 */

int print_sign(int n)
{
	if (n > 0)
	{
		return (1);
	}
	else if (n == 0)
	{
		return (0);
	}
	else
	{
		return (-1);
	}

}
