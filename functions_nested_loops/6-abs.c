#include "main.h"
#include <stdio.h>

/**
 * _abs - Entry point
 * @n: input
 * Description: _abs computes the absolute value of an integer.
 * Return: Always 0.
 */

int _abs(int n)
{
	if (n < 0)
	{
		n = -1 * n;
		_putchar('n');
	}
	else
	{
		_putchar('n');
	}
		return (0);
}
